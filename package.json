{
  "name": "biometric-sync",
  "version": "1.0.0",
  "description": "Dahua face recognition device log sync with gap-fill",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test-device": "node -e \"require('./device-client').gapFill().then(r => console.log('Done:', r.length, 'records'))\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
